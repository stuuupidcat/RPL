rpl_patterns_unsound_as_bytes_method = method `{$name}` casts `{$ref_mutbly}self` to `{$ref_mutbly}[u8]`

rpl_patterns_unsound_as_bytes_trait = it is unsound to cast any type to a slice
    .suggestion = change this trait to an `unsafe` trait
